# Configuración de Red - Batalla Naval
# Para despliegue en red local

# IP o hostname del servidor
# Si el servidor y clientes están en la misma red:
# - Windows: Usa la dirección IPv4 (ej. 192.168.1.100)
# - Linux/Mac: Usa hostname o IP
server.host=localhost

# Puerto del servidor
server.port=9090

# Base de datos (solo usado en el servidor)
db.url=jdbc:postgresql://localhost:54322/battleship_db
db.user=postgres
db.password=admin
